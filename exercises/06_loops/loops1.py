# I AM NOT DONE

def practice(numbers):
    # TODO: Create a variable `sum` initialized to 0
    
    # TODO: Use a for loop to iterate through `numbers` and add each number to `sum`
    
    return locals()
