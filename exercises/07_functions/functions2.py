# I AM NOT DONE

def practice():
    # TODO: Define a function named `add` that takes two arguments `a` and `b` and returns their sum
    
    # TODO: Call the `add` function with 5 and 3, and assign the result to `result`
    
    return locals()
