# I AM NOT DONE

def practice(value):
    # TODO: Use a try/except block to handle the potential error
    # TODO: Try to convert `value` to an integer and return it
    # TODO: If a ValueError occurs (e.g., "abc" cannot be converted), return -1
    
    return int(value)
